#pragma once
#include "Graph.h"
#include "LinkMatrixGraph.h"


int main()
{
	/*FLinkMatrixGraph* MatrixGraph = new FLinkMatrixGraph(5);
	MatrixGraph->AddEdge(0, 1);
	MatrixGraph->AddEdge(0, 2);
	MatrixGraph->AddEdge(0, 4);
	MatrixGraph->AddEdge(1, 4);
	MatrixGraph->AddEdge(3, 2);

	MatrixGraph->PrintMatrix();*/


	system("pause"); 
	return 0;
}